# ThreatLandscape Configuration
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control

# Application
HOST=0.0.0.0
PORT=5000
DATA_DIR=./data

# API keys (OpenAI / Anthropic / Malpedia), SMTP settings, and the
# notification recipient are configured in the Settings page of the web UI
# and stored in data/config.json. They are NOT read from the environment.
